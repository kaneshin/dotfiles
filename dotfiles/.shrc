# vim:set ts=8 sts=2 sw=2 tw=0:
# vim:set fdm=marker:
#
# File:        .shrc
# Maintainer:  Shintaro Kaneko <kaneshin0120@gmail.com>
# Last Change: 02-Feb-2015.
# ============================================================

# alias
alias ce='cd ../'

alias ls='ls -F'
alias la='ls -a'
alias ll='ls -la'
alias sl="ls"
alias l='ls -la'

alias grep='grep --color=always'
alias ngrep='grep -n --color=always'
alias fgrep='fgrep --color=always'
alias egrep='egrep --color=always'

alias less='less -R'
alias les='less -R'

set_gitst()
{
  case $1 in
    on )
      GITST=1
      ;;
    off )
      GITST=0
      ;;
  esac
}

set_gitst on
alias gitst='set_gitst'
parse_git_status()
{
  if [ $GITST -ne 0 ]; then
    git status -s 2> /dev/null
  fi
}

parse_git_branch()
{
  git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ \1/'
}

function gi()
{
  curl https://www.gitignore.io/api/$@ ;
}


# OS
case $OSTYPE in
  darwin* )
    if [ -f ~/.shrc.darwin ]; then
      . ~/.shrc.darwin
    fi
    ;;
  linux* )
    if [ -f ~/.shrc.linux ]; then
      . ~/.shrc.linux
    fi
    ;;
esac

# local
if [ ! -f ~/.shrc.local ]; then
  touch ~/.shrc.local
fi
if [ -f ~/.shrc.local ]; then
  . ~/.shrc.local
fi

