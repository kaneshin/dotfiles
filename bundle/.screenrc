# vim:set fdm=marker:
#
# File:        .screenrc
# Maintainer:  Shintaro Kaneko <kaneshin0120@gmail.com>
# Last Change: 14-Dec-2015.
#
# help (?) - Screen help
#
# Useful keys
#   screen (c) - generate new window and shell
#   next (n) - next window
#   prev (p) - previous window
#   select (') - switch window
#   windowlist (") - switch window
#   windows (w) - window list
#   title (A) - change window title
#   kill (K) - kill window and shell
#   redisplay (l) - redraw current window

# encoding
encoding utf8
defencoding utf8
defkanji utf8

# do you wanna see the copyright? I don't wanna do.
startup_message off

# automatically detach if hung-up
autodetach on

# viewable bell
vbell off

# default size of scrollback buffer
defscrollback 5000

# terminal
term screen-256color

# shell title
shelltitle "$|zsh"

# status line
hardstatus alwayslastline "%{= wk}[screen@$USER]%` %`%-w%{= ck}%n %t%{-}%+w %=[$LANG][%m/%d %02c:%s]"

# escape
escape 

# key bind with escape
bind 0 remove
bind 1 only
bind 2 split
bind 3 split -v
bind j screen
# bind  focus

